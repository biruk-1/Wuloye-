/**
 * Replace YOUR_LOCAL_IP with your machine's LAN IP (same network as the device/emulator).
 * Android emulator: use 10.0.2.2 instead of localhost to reach the host machine.
 */
export const API_BASE_URL = 'http://YOUR_LOCAL_IP:5000/api';
