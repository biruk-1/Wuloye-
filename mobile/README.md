# Wuloye — Mobile (React Native / Expo)

> Sprint 2+

This directory will contain the React Native (Expo) mobile application.

## Planned Setup

```bash
npx create-expo-app@latest . --template blank-typescript
```
